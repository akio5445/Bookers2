<%= form_for(book) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved: not successfully</h2>
      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p id="notice"><%= notice %></p>

  
  <table class="table">
  <tr>
  <div class="field">
  <th><%= form.label :Title %></th>
  <th><%= form.text_field :title %></th>
  </div>
  </tr>
  <tr>
  <div class="field">
    <th><%= form.label :Opinion %></th>
    <th><%= form.text_area :body %></th>
  </div>
  </tr>
  <tr>
  <div class="actions">
    <th><%= form.submit "Create Book" %></th>
  </div>
  </tr>
  </table>
<% end %>